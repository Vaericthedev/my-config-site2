---
// This runs on the server before the page loads
const GITHUB_USERNAME = "Vaericthedev";
const REPO_NAME = "my-config-site2";
const FOLDER_NAME = "Roblox"; 
---

<html lang="en">
<head>
    <meta charset="UTF-8" />
    <title>Vixix Configs</title>
    <style>
        /* ========================================= */
        /* 1. THEME VARIABLES (WITH BACKGROUNDS)     */
        /* ========================================= */
        
        /* Midnight (Default) - Dark mountain night */
        :root, body.theme-midnight {
            --bg: #0d1117;
            --bg-image: url('https://images.unsplash.com/photo-1519681393784-d120267933ba?q=80&w=2070&auto=format&fit=crop');
            --bg-overlay: rgba(13, 17, 23, 0.8);
            --card-bg: rgba(22, 27, 34, 0.8);
            --border: #30363d;
            --text: #c9d1d9;
            --text-muted: #8b949e;
            --text-bright: #f0f6fc;
            --input-bg: rgba(22, 27, 34, 0.9);
            --btn-bg: rgba(33, 38, 45, 0.9);
            --btn-hover: rgba(48, 54, 61, 0.9);
            --btn-text: #c9d1d9;
        }

        /* Abyss - Vibrant Nebula */
        body.theme-abyss {
            --bg: #000000;
            --bg-image: url('https://images.unsplash.com/photo-1462331940025-496dfbfc7564?q=80&w=2000&auto=format&fit=crop');
            --bg-overlay: rgba(0, 0, 0, 0.65); /* Lower opacity so stars show */
            --card-bg: rgba(17, 17, 17, 0.65); /* More transparent for glass effect */
            --border: #222222;
            --text: #eeeeee;
            --text-muted: #888888;
            --text-bright: #ffffff;
            --input-bg: rgba(17, 17, 17, 0.8);
            --btn-bg: rgba(34, 34, 34, 0.8);
            --btn-hover: rgba(51, 51, 51, 0.8);
            --btn-text: #eeeeee;
        }

        /* Ghost - Bright Modern Architecture */
        body.theme-ghost {
            --bg: #f6f8fa;
            --bg-image: url('https://images.unsplash.com/photo-1497366811353-6870744d04b2?q=80&w=2000&auto=format&fit=crop');
            --bg-overlay: rgba(246, 248, 250, 0.65); /* Lower opacity so architecture shows */
            --card-bg: rgba(255, 255, 255, 0.7); /* More transparent for glass effect */
            --border: #d0d7de;
            --text: #24292f;
            --text-muted: #57606a;
            --text-bright: #000000;
            --input-bg: rgba(255, 255, 255, 0.8);
            --btn-bg: rgba(246, 248, 250, 0.8);
            --btn-hover: rgba(234, 238, 242, 0.8);
            --btn-text: #24292f;
        }

        /* Vixix - Neon Cyberpunk */
        body.theme-vixix {
            --bg: #120f1c;
            --bg-image: url('https://images.unsplash.com/photo-1550684848-fac1c5b4e853?q=80&w=2070&auto=format&fit=crop');
            --bg-overlay: rgba(18, 15, 28, 0.8);
            --card-bg: rgba(28, 23, 39, 0.8);
            --border: #2d253a;
            --text: #e0d8f0;
            --text-muted: #8b7fa8;
            --text-bright: #ffffff;
            --input-bg: rgba(28, 23, 39, 0.9);
            --btn-bg: rgba(45, 37, 58, 0.9);
            --btn-hover: rgba(62, 51, 80, 0.9);
            --btn-text: #e0d8f0;
        }

        /* ========================================= */
        /* 2. BASE RESET & BACKGROUND ENGINE         */
        /* ========================================= */
        *, *::before, *::after { box-sizing: border-box; }
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
            background-color: var(--bg);
            color: var(--text);
            margin: 0;
            padding: 40px 20px;
            line-height: 1.5;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            transition: background-color 0.3s ease, color 0.3s ease;
        }

        /* The Blurred Background Layer */
        #bg-layer {
            position: fixed;
            top: -20px;
            left: -20px;
            width: calc(100% + 40px);
            height: calc(100% + 40px);
            z-index: -2;
            background-image: var(--bg-image);
            background-size: cover;
            background-position: center;
            filter: blur(12px); /* Slightly increased blur for the glass effect */
            transition: background-image 0.5s ease;
        }

        /* The Dark/Light Overlay Layer */
        #bg-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            background-color: var(--bg-overlay);
            transition: background-color 0.5s ease;
        }

        .container {
            width: 100%;
            max-width: 900px;
            background: var(--card-bg);
            backdrop-filter: blur(16px);
            -webkit-backdrop-filter: blur(16px);
            border: 1px solid var(--border);
            border-radius: 16px;
            padding: 24px;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.2);
            transition: background-color 0.3s ease, border-color 0.3s ease;
        }

        /* 3. HEADER */
        .header {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            margin-bottom: 32px;
            border-bottom: 1px solid var(--border);
            padding-bottom: 16px;
            transition: border-color 0.3s ease;
        }
        .header-text h1 { 
            margin: 0 0 8px 0; 
            font-size: 24px; 
            font-weight: 600;
            color: var(--text-bright); 
        }
        .header-text p { 
            margin: 0; 
            color: var(--text-muted); 
            font-size: 14px; 
        }

        /* 4. THEME SWITCHER */
        .theme-switcher {
            padding: 8px 12px;
            background-color: var(--input-bg);
            border: 1px solid var(--border);
            border-radius: 6px;
            color: var(--text);
            font-family: inherit;
            font-size: 13px;
            outline: none;
            cursor: pointer;
            backdrop-filter: blur(8px);
            transition: border-color 0.2s ease, background-color 0.3s ease;
        }
        .theme-switcher:focus {
            border-color: #58a6ff;
        }

        /* 5. SEARCH AND SORT CONTROLS */
        .controls { 
            display: flex; 
            gap: 12px; 
            margin-bottom: 24px; 
        }
        
        input, select {
            flex: 1;
            padding: 10px 14px;
            background-color: var(--input-bg);
            border: 1px solid var(--border);
            border-radius: 8px;
            color: var(--text);
            font-family: inherit;
            font-size: 14px;
            outline: none;
            backdrop-filter: blur(8px);
            transition: border-color 0.2s ease, background-color 0.3s ease;
        }

        input:focus, select:focus {
            border-color: #58a6ff; 
        }

        /* 6. CONFIG GRID & CARDS */
        .config-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
            gap: 16px;
        }

        .config-card {
            background-color: var(--card-bg);
            backdrop-filter: blur(8px);
            -webkit-backdrop-filter: blur(8px);
            border: 1px solid var(--border);
            border-radius: 12px;
            padding: 16px;
            display: flex;
            flex-direction: column;
            gap: 16px;
            transition: border-color 0.2s ease, background-color 0.3s ease;
            align-items: flex-start; 
        }

        .config-card:hover {
            border-color: var(--text-muted); 
        }

        .config-card h3 { 
            margin: 0; 
            word-break: break-all; 
            font-size: 14px;
            font-weight: 500;
            color: var(--text-bright);
        }

        /* ========================================= */
        /* 7. THE SIMPLE, UNBREAKABLE BUTTON         */
        /* ========================================= */
        .download-btn {
            /* Nuke all browser defaults */
            -webkit-appearance: none !important;
            appearance: none !important;
            
            /* Use theme variables */
            background-color: var(--btn-bg) !important;
            border: 1px solid var(--border) !important;
            border-radius: 6px !important;
            color: var(--btn-text) !important;
            
            /* Sizing */
            display: inline-block !important;
            width: auto !important;
            padding: 6px 12px !important;
            margin: 0 !important;
            
            /* Text */
            font-family: inherit !important;
            font-size: 12px !important;
            font-weight: 500 !important;
            text-decoration: none !important;
            cursor: pointer !important;
            outline: none !important;
            
            transition: all 0.1s ease !important;
        }

        .download-btn:hover {
            background-color: var(--btn-hover) !important;
            color: var(--text-bright) !important;
            border-color: var(--text-muted) !important;
        }

        .download-btn:active {
            transform: scale(0.98) !important;
        }

        #loading { 
            color: var(--text-muted); 
            font-size: 14px;
            text-align: center;
            padding: 20px;
        }
    </style>
</head>
<body class="theme-midnight">
    
    <!-- BACKGROUND LAYERS -->
    <div id="bg-layer"></div>
    <div id="bg-overlay"></div>

    <div class="container">
        <div class="header">
            <div class="header-text">
                <h1>Roblox Configs</h1>
                <p>Download the latest premium configs below.</p>
            </div>
            <!-- THEME SWITCHER -->
            <select id="themeSwitcher" class="theme-switcher">
                <option value="theme-midnight">Midnight</option>
                <option value="theme-abyss">Abyss</option>
                <option value="theme-ghost">Ghost</option>
                <option value="theme-vixix">Vixix</option>
            </select>
        </div>

        <div class="controls">
            <input type="text" id="searchBar" placeholder="Search configs..." oninput="filterConfigs()" />
            <select id="sortSelect" onchange="sortConfigs()">
                <option value="name-asc">Sort by Name (A-Z)</option>
                <option value="name-desc">Sort by Name (Z-A)</option>
            </select>
        </div>

        <p id="loading">Loading configs from GitHub... ⏳</p>
        <div class="config-grid" id="configGrid"></div>
    </div>

    <script>
        // ==========================================
        // THEME SWITCHER LOGIC
        // ==========================================
        const savedTheme = localStorage.getItem('vixix-theme');
        if (savedTheme) {
            document.body.className = savedTheme;
            document.getElementById('themeSwitcher').value = savedTheme;
        }

        document.getElementById('themeSwitcher').addEventListener('change', (e) => {
            const newTheme = e.target.value;
            document.body.className = newTheme;
            localStorage.setItem('vixix-theme', newTheme);
        });

        // ==========================================
        // CONFIG FETCHING & RENDERING
        // ==========================================
        const API_URL = `https://api.github.com/repos/Vaericthedev/my-config-site2/contents/Roblox`;
        let allConfigs = [];

        async function fetchConfigs() {
            try {
                const response = await fetch(API_URL);
                const data = await response.json();
                
                allConfigs = data.filter(file => {
                    const name = file.name.toLowerCase();
                    return name.endsWith('.json') || name.endsWith('.cfg') || name.endsWith('.txt');
                });

                document.getElementById('loading').style.display = 'none';
                renderConfigs(allConfigs);
            } catch (error) {
                document.getElementById('loading').innerText = "Error loading configs. Make sure the folder exists on GitHub!";
            }
        }

        function renderConfigs(configs) {
            const grid = document.getElementById('configGrid');
            grid.innerHTML = '';

            if (configs.length === 0) {
                grid.innerHTML = '<p style="color: var(--text-muted); grid-column: 1/-1;">No configs found.</p>';
                return;
            }

            configs.forEach(file => {
                const card = document.createElement('div');
                card.className = 'config-card';

                const title = document.createElement('h3');
                title.textContent = file.name;

                const btn = document.createElement('button');
                btn.type = 'button';
                btn.className = 'download-btn';
                btn.textContent = 'Download'; 
                
                btn.addEventListener('click', () => {
                    forceDownload(file.download_url + '?raw=true', file.name);
                });

                card.appendChild(title);
                card.appendChild(btn);
                grid.appendChild(card);
            });
        }

        async function forceDownload(url, filename) {
            try {
                const response = await fetch(url);
                const blob = await response.blob();
                const blobUrl = window.URL.createObjectURL(blob);
                
                const a = document.createElement('a');
                a.style.display = 'none';
                a.href = blobUrl;
                a.download = filename;
                document.body.appendChild(a);
                a.click();
                
                window.URL.revokeObjectURL(blobUrl);
                document.body.removeChild(a);
            } catch (error) {
                console.error('Download failed:', error);
                window.open(url, '_blank');
            }
        }

        function filterConfigs() {
            const query = document.getElementById('searchBar').value.toLowerCase();
            const filtered = allConfigs.filter(file => file.name.toLowerCase().includes(query));
            renderConfigs(filtered);
        }

        function sortConfigs() {
            const sortValue = document.getElementById('sortSelect').value;
            const sorted = [...allConfigs].sort((a, b) => {
                if (sortValue === 'name-asc') return a.name.localeCompare(b.name);
                if (sortValue === 'name-desc') return b.name.localeCompare(a.name);
                return 0;
            });
            renderConfigs(sorted);
        }

        fetchConfigs();
    </script>
</body>
</html>
